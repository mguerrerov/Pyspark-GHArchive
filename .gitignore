# Datos: nunca al repo. El lago vive en GHA_DATA_DIR (por defecto D:/gharchive-data).
data/
*.json.gz
*.ndjson
*.part

# DuckDB: el fichero de trabajo local no se commitea.
# Los parquets agregados de gold si entran (ver dbt/target/gold_publicado/).
*.duckdb
*.duckdb.wal

# Python
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ipynb_checkpoints/

# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/
.user.yml

# Evidence / Node
node_modules/
dashboard/.evidence/
dashboard/build/

# Sitio estatico: el CSS compilado por Tailwind y el ensamblado de Netlify
# se generan en el build (construir_sitio.sh).
site/assets/app.css
dist/
# Los JSON del sitio si van al repo: los genera exportar_sitio.py en local y
# Netlify no tiene con que regenerarlos. La regla `data/` de arriba los
# tapaba.
!site/data/

# Spark
spark-warehouse/
metastore_db/
derby.log

# Power BI: los .pbix si se commitean (son el entregable); los temporales no.
*.pbix.tmp

# Sistema
.DS_Store
Thumbs.db
.idea/
.vscode/

# Salidas de consola de los scripts de verificacion. El resultado que vale
# se guarda en el .json de al lado y en docs/metrics.md.
docs/*_salida.txt
docs/*_error.txt
